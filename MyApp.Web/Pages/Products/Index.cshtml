 @page
@model IndexModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "E-Bazaar";
}
@section Styles {
    <link rel="stylesheet" href="~/css/home.css" />
}

<header class="header">
  <div class="container">
    <div class="header-content">
      <div class="logo">E-Bazaar</div>
      <div class="cart"><div class="cart-icon"></div>Cart (@Model.CartCount)</div>
    </div>
  </div>
</header>

<main class="container">
  <!-- Stats Section -->
  <section class="stats-section">
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-icon">🛍️</div>
        <div class="stat-content">
          <h3>Total Products: @Model.TotalProducts</h3>
          <p>Maximum sale of all the products<br />during the last month of a year</p>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon">👥</div>
        <div class="stat-content">
          <h3>Total Vendors: @Model.TotalVendors</h3>
          <p>A total of @Model.TotalVendors vendors are<br />available in our shop</p>
        </div>
      </div>
      <div class="stat-card">
        <div class="stat-icon">📦</div>
        <div class="stat-content">
          <h3>Unique Products: @Model.UniqueProducts</h3>
          <p>Recognized as unique products<br />in our catalog</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Search Section -->
  <section class="search-section">
    <div class="search-bar">
      <input type="text" class="search-input" placeholder="Search products…" value="@Model.SearchText" />
      <button class="search-btn">Search</button>
      <button class="add-product-btn" onclick="home.openModal()">Add Product</button>
    </div>
    <div class="results-info">
      Showing @Model.StartResult–@Model.EndResult of @Model.TotalResults results
    </div>
  </section>

  <!-- Products Grid -->
  <section class="products-section">
    <div class="products-grid">
      @foreach(var p in Model.Products) {
      <div class="product-card">
        <img src="@p.ProductImage" alt="@p.ProductName" class="product-image" />
        <div class="product-name">@p.ProductName</div>
        <div class="product-price">
          @p.Price.ToString("C")
          @if(p.DiscountedPrice < p.Price){
            <span style="text-decoration: line-through; color: #999; font-size: 14px;";">@p.Price.ToString("C")</span>
          }
        </div>
        <div class="cart-controls">
          <div class="quantity-controls">
            <div class="quantity-row">
              <button class="quantity-btn" onclick="home.decreaseQuantity(this)">-</button>
              <span class="quantity-display">1</span>
              <button class="quantity-btn" onclick="home.increaseQuantity(this)">+</button>
            </div>
            <button class="confirm-add-btn" onclick="home.addToCart(this)">Add to Cart</button>
          </div>
          <button class="add-to-cart">Add to cart</button>
        </div>
      </div>
      }
    </div>

    <!-- Pagination -->
    <div class="pagination">
      <button onclick="home.prevPage()">← Prev</button>
      @for(int i=1; i<=Model.TotalPages; i++){
        <button class="@(i==Model.CurrentPage?"active":"")" onclick="home.goToPage(@i)">@i</button>
      }
      <span class="dots">…</span>
      <button onclick="home.nextPage()">Next →</button>
      <div class="per-page-select">
        <button class="per-page-btn" id="perPageBtn">@Model.PageSize per page</button>
        <div class="per-page-dropdown">
          <button class="per-page-option" onclick="home.changePerPage(5)">5 per page</button>
          <button class="per-page-option" onclick="home.changePerPage(10)">10 per page</button>
          <button class="per-page-option" onclick="home.changePerPage(20)">20 per page</button>
          <button class="per-page-option" onclick="home.changePerPage(50)">50 per page</button>
        </div>
      </div>
    </div>
  </section>
</main>

<!-- Add Product Modal -->
<div class="modal-overlay" id="modalOverlay" onclick="home.closeModalOnOverlay(event)">
  <div class="modal">
    <div class="modal-header">
      <div>
        <h2 class="modal-title">Add New Product</h2>
        <p class="modal-subtitle">Enter details for your new product.</p>
      </div>
      <button class="modal-close" onclick="home.closeModal()">×</button>
    </div>
    <div class="modal-body">
      <form id="addProductForm">
        <div class="form-group">
          <label class="form-label">Product Name</label>
          <input type="text" id="productName" class="form-input" oninput="home.generateSlug()" />
        </div>
        <div class="form-group">
          <label class="form-label">Slug</label>
          <div class="form-row">
            <input type="text" id="productSlug" class="form-input" />
            <button type="button" class="generate-btn" onclick="home.generateSlug()">Generate</button>
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Price</label>
          <input type="text" id="productPrice" class="form-input" />
        </div>
        <div class="form-group">
          <label class="form-label">Discount Start</label>
          <div class="date-input-wrapper">
            <input type="date" id="discountStart" class="date-input" value="@DateTime.UtcNow:yyyy-MM-dd" />
            <div class="calendar-icon"></div>
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Discount End</label>
          <div class="date-input-wrapper">
            <input type="date" id="discountEnd" class="date-input" value="@DateTime.UtcNow.AddDays(1):yyyy-MM-dd" />
            <div class="calendar-icon"></div>
          </div>
        </div>
      </form>
    </div>
    <div class="modal-footer">
      <button class="add-btn" onclick="home.submitProduct()">Add</button>
    </div>
  </div>
</div>

<!-- Shopping Cart Sidebar -->
<div class="cart-sidebar-overlay" id="cartSidebarOverlay" onclick="home.closeCartSidebar(event)">
  <div class="cart-sidebar">
    <div class="cart-header">
      <h3 class="cart-title">Shopping Cart</h3>
      <button class="cart-close" onclick="home.closeCartSidebar()">×</button>
    </div>
    <div class="cart-items" id="cartItems">
      <div class="cart-item">
        <img src="/placeholder.svg?height=60&width=60" class="cart-item-image" />
        <div class="cart-item-details">
          <div class="cart-item-name">DJI Phantom 3 Vision+</div>
          <div class="cart-item-price">$499.00</div>
          <div class="cart-item-quantity">
            <span>Qty: </span>
            <button class="qty-btn" onclick="home.updateCartQuantity(this, -1)">-</button>
            <span class="qty-number">1</span>
            <button class="qty-btn" onclick="home.updateCartQuantity(this, +1)">+</button>
          </div>
        </div>
      </div>
      <div class="cart-item">
        <img src="/placeholder.svg?height=60&width=60" class="cart-item-image" />
        <div class="cart-item-details">
          <div class="cart-item-name">DJI Phantom 3 Vision+</div>
          <div class="cart-item-price">$499.00</div>
          <div class="cart-item-quantity">
            <span>Qty: </span>
            <button class="qty-btn" onclick="home.updateCartQuantity(this, -1)">-</button>
            <span class="qty-number">1</span>
            <button class="qty-btn" onclick="home.updateCartQuantity(this, +1)">+</button>
          </div>
        </div>
      </div>
      <div class="cart-item">
        <img src="/placeholder.svg?height=60&width=60" class="cart-item-image" />
        <div class="cart-item-details">
          <div class="cart-item-name">DJI Phantom 3 Vision+</div>
          <div class="cart-item-price">$499.00</div>
          <div class="cart-item-quantity">
            <span>Qty: </span>
            <button class="qty-btn" onclick="home.updateCartQuantity(this, -1)">-</button>
            <span class="qty-number">1</span>
            <button class="qty-btn" onclick="home.updateCartQuantity(this, +1)">+</button>
          </div>
        </div>
      </div>
      <div class="cart-item">
        <img src="/placeholder.svg?height=60&width=60" class="cart-item-image" />
        <div class="cart-item-details">
          <div class="cart-item-name">DJI Phantom 3 Vision+</div>
          <div class="cart-item-price">$499.00</div>
          <div class="cart-item-quantity">
            <span>Qty: </span>
            <button class="qty-btn" onclick="home.updateCartQuantity(this, -1)">-</button>
            <span class="qty-number">1</span>
            <button class="qty-btn" onclick="home.updateCartQuantity(this, +1)">+</button>
          </div>
        </div>
      </div>
    </div>
    <div class="cart-footer">
      <div class="cart-subtotal">
        <span class="subtotal-label">Subtotal</span>
        <span class="subtotal-amount" id="cartSubtotal">$0.00</span>
      </div>
    </div>
  </div>
</div>

@section Scripts {
    <script src="~/js/home.js"></script>
}
